pool: kvm
zone: client

power_type: manual
power_parameters: "{}"

# 例如 ipmi:
# power_type: ipmi
# power_parameters: '{"power_address":"192.168.1.100","power_user":"admin","power_pass":"password"}'

# 例如 virsh:
# power_type: virsh
# power_parameters: '{"uri":"qemu+ssh://root@host/system"}'